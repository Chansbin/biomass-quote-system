# 云端部署配置
# 使用 Node.js 运行时

node_version = 22

[build]
commands = [
    "cd server && npm install",
    "npm install"
]

[start]
command = "node server/index.js"

[env]
NODE_ENV = "production"
PORT = 3001

[volumes.data-storage]
mount = "/app/data"